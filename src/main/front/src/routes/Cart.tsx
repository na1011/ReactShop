import React from 'react';
import CartTable from '../components/CartTable';

const Cart: React.FC = () => {
    return (
        <div>
            <CartTable />
        </div>
    );
};

export default Cart;
